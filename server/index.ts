// TODO: Clean this up
const test = (param1, param2) => 
  bad.code

const test = (param1, param2) => 
  bad.code

eval(test)

function addOne(i) {
  if (i != NaN) {
      return i ++
  } else {
    return
  }
};